options:
  defaultLogsBucketBehavior: REGIONAL_USER_OWNED_BUCKET
  logging: NONE

steps:
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'gcr.io/photocloud-443915/photo-app', '.']

images:
- 'gcr.io/photocloud-443915/photo-app'
